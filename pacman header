#include <iostream>
#include <SFML/Graphics.hpp>
#include <SFML/System/Export.hpp>
#include <SFML/System/Time.hpp>
#include <iostream>
#include <fstream>
#include "pellets.h"
class pacman :public pellets
{
private:
	struct move
	{
		bool up = true;
		bool down = true;
		bool right = true;
		bool left = true;
	}move;
	Texture pacTexture;
	Sprite pac;
	float movementSpeed = 1;
	int direction = 0;
	RectangleShape rect;
public:
	pacman(Sprite& pac, Texture& pacTexture, Vector2f postion, Vector2f scale, const char* file);
	void update();
	void updateMovement();
};
