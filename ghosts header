#include <iostream>
#include <SFML/Graphics.hpp>
#include <SFML/System/Export.hpp>
#include <SFML/System/Time.hpp>
#include <iostream>
#include <fstream>
#include <string>
using namespace std;
using namespace sf;
class ghosts
{
private:
	Clock clock;
	Texture texture;
	RectangleShape blink;
	RectangleShape ink;
	RectangleShape pink;
	RectangleShape clyd;
	float row;
	float column;
public:
	void blinky(float initialRow, float initialColumn, string imageName);
	void inky(float initialRow, float initialColumn, string imageName);
	void pinky(float initialRow, float initialColumn, string imageName);
	void clyde(float initialRow, float initialColumn, string imageName);
};
